import{describe,expect,it}from'vitest';
import{rankCompanions}from'./companionDiscovery';
describe('companion discovery',()=>{const input=[{id:'1',name:'A',slug:'a',occupation:'',summary:'',traits:[],interests:['Books'],goals:['Social worlds']},{id:'2',name:'B',slug:'b',occupation:'',summary:'',traits:[],interests:['Music'],goals:[]}];it('ranks server-provided shared goals before incidental interests',()=>{const ranked=rankCompanions(['Social worlds'],['Music'],input);expect(ranked[0]?.id).toBe('1');});it('does not mutate the server catalog',()=>{rankCompanions([],['Music'],input);expect(input[0]?.id).toBe('1');});});

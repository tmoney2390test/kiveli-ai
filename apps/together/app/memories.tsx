import { Alert, Pressable, StyleSheet, Switch, Text, View } from 'react-native';
import { router } from 'expo-router';
import { ArrowLeft, BellRing, Heart, Pin, Sparkles, UserRound } from 'lucide-react-native';
import { EmptyState, PageTitle, Screen } from '../src/components';
import { colors, radius } from '../src/theme';
import { useTogether } from '../src/store/useTogether';
import { mutateMemory } from '../src/lib/api';
import type { Memory } from '../src/types';

const tabs=[['About You',['semantic','emotional']],['Preferences',['preference']],['Shared',['episodic']],['Upcoming',['open_thread']],['Relationship',['relationship']]] as const;
const categoryControls: Array<[string,string]>=[['semantic','About you'],['preference','Preferences'],['episodic','Shared experiences'],['emotional','Emotional context'],['open_thread','Upcoming things']];
export default function Memories(){const{snapshot,refresh}=useTogether();const memories=snapshot?.memories??[];const categories=snapshot?.profile?.memory_categories??{};const update=async(action:string,memory:Memory)=>{await mutateMemory({action,memoryId:memory.id,...(action==='pin'?{pinned:!memory.pinned}:{})});await refresh();};const toggleCategory=async(key:string,value:boolean)=>{await mutateMemory({action:'preferences',categories:{...categories,[key]:value}});await refresh();};const menu=(memory:Memory)=>Alert.alert('Memory controls',memory.canonical_text,[{text:memory.pinned?'Unpin':'Pin',onPress:()=>void update('pin',memory)},{text:'Edit',onPress:()=>Alert.prompt?.('Edit memory',undefined,(text)=>text&&void mutateMemory({action:'edit',memoryId:memory.id,text}).then(refresh),undefined,memory.canonical_text)},{text:'Forget',style:'destructive',onPress:()=>void update('forget',memory)},{text:'Cancel',style:'cancel'}]);return <Screen><View style={styles.header}><Pressable onPress={()=>router.back()}><ArrowLeft color={colors.text}/></Pressable><PageTitle>What Maya Remembers</PageTitle></View>{memories.length?tabs.map(([label,types])=>{const group=memories.filter((memory)=>types.includes(memory.memory_type as never));if(!group.length)return null;return <View key={label} style={{gap:8}}><Text style={styles.section}>{label}</Text>{group.map((memory)=><Pressable key={memory.id} onPress={()=>menu(memory)} style={styles.memory}><MemoryIcon type={memory.memory_type}/><Text style={styles.text}>{memory.canonical_text}</Text>{memory.pinned?<Pin size={14} color={colors.rose} fill={colors.rose}/>:null}<Text style={styles.date}>{new Date(memory.updated_at).toLocaleDateString(undefined,{month:'short',day:'numeric'})}</Text></Pressable>)}</View>}):<EmptyState title="Maya is listening" body="Meaningful details you share will appear here. You stay in control: edit, pin, or forget any memory."/>}<Text style={styles.section}>Memory preferences</Text>{categoryControls.map(([key,label])=><View key={key} style={styles.control}><Text style={styles.text}>{label}</Text><Switch value={categories[key]!==false} onValueChange={(value)=>void toggleCategory(key,value)} trackColor={{false:colors.elevated,true:colors.rose}}/></View>)}</Screen>;}
function MemoryIcon({type}:{type:string}){const props={size:18,color:colors.warm};if(type==='preference')return <Heart {...props}/>;if(type==='episodic')return <Sparkles {...props}/>;if(type==='open_thread')return <BellRing {...props}/>;return <UserRound {...props}/>;}
const styles=StyleSheet.create({header:{flexDirection:'row',alignItems:'center',gap:14},section:{color:colors.rose,fontWeight:'800',fontSize:12,letterSpacing:.8,marginTop:8},memory:{minHeight:62,flexDirection:'row',alignItems:'center',gap:11,padding:12,borderRadius:radius.md,backgroundColor:colors.surface,borderWidth:1,borderColor:colors.border},control:{minHeight:54,flexDirection:'row',alignItems:'center',gap:11,paddingHorizontal:12,borderRadius:radius.md,backgroundColor:colors.surface},text:{flex:1,color:colors.text,lineHeight:19},date:{color:colors.muted,fontSize:10}});

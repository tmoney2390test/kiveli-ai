import{useEffect,useState}from'react';import{Alert,Pressable,StyleSheet,Text,TextInput,View}from'react-native';import{router,useLocalSearchParams}from'expo-router';import{ArrowLeft,Trash2}from'lucide-react-native';import{GradientButton,LoadingSkeleton,PageTitle,Screen}from'../src/components';import{managePersona}from'../src/lib/api';import{useTogether}from'../src/store/useTogether';import{colors,radius}from'../src/theme';import type{UserPersona}from'../src/types';
export default function PersonaEditor(){const params=useLocalSearchParams<{persona?:string}>(),{snapshot,setCoreState}=useTogether(),existing=snapshot?.personas?.find((item)=>item.id===params.persona);const[name,setName]=useState(''),[pronouns,setPronouns]=useState(''),[age,setAge]=useState(''),[occupation,setOccupation]=useState(''),[about,setAbout]=useState(''),[interests,setInterests]=useState(''),[busy,setBusy]=useState(false);useEffect(()=>{if(!existing)return;setName(existing.display_name);setPronouns(existing.pronouns??'');setAge(existing.age?String(existing.age):'');setOccupation(existing.occupation??'');setAbout(existing.biography??'');setInterests(existing.interests.join(', '));},[existing]);if(!snapshot)return <LoadingSkeleton/>;const save=async()=>{setBusy(true);try{const payload={displayName:name.trim(),pronouns:pronouns.trim()||null,age:age?Number(age):null,occupation:occupation.trim()||null,biography:about.trim()||null,interests:interests.split(',').map((item)=>item.trim()).filter(Boolean),communicationConfig:existing?.communication_config??{},metadata:existing?.metadata??{}};const saved=await managePersona<UserPersona>(existing?{action:'update',personaId:existing.id,...payload}:{action:'create',...payload});const personas=existing?(snapshot.personas??[]).map((item)=>item.id===saved.id?saved:item):[...(snapshot.personas??[]),saved];setCoreState({personas,activePersona:snapshot.activePersona?.id===saved.id?saved:snapshot.activePersona,continuities:(snapshot.continuities??[]).map((life)=>life.persona_id===saved.id?{...life,together_user_personas:saved}:life)});router.replace('/personas');}catch(error){Alert.alert(`Could not ${existing?'update':'create'} Persona`,error instanceof Error?error.message:'Please try again.');}finally{setBusy(false)}};const remove=()=>existing&&!existing.is_default?Alert.alert(`Delete ${existing.display_name}?`,'This is only available when the Persona has no Kivelle Life or relationship history.',[{text:'Keep persona',style:'cancel'},{text:'Delete persona',style:'destructive',onPress:async()=>{setBusy(true);try{await managePersona({action:'delete_persona',personaId:existing.id,confirmation:'DELETE PERSONA'});setCoreState({personas:(snapshot.personas??[]).filter((item)=>item.id!==existing.id)});router.replace('/personas');}catch(error){Alert.alert('Persona could not be deleted',error instanceof Error?error.message:'Delete its Alternate Lives first.');}finally{setBusy(false)}}}]):undefined;return <Screen><View style={styles.header}><Pressable onPress={()=>router.back()}><ArrowLeft color={colors.text}/></Pressable><View><PageTitle>{existing?'Edit persona':'Create persona'}</PageTitle><Text style={styles.subtitle}>Who are you in this Life?</Text></View></View><Label text="What should companions call you?"/><TextInput value={name} onChangeText={setName} style={styles.input} placeholder="Jordan" placeholderTextColor={colors.muted}/><View style={styles.row}><View style={{flex:1}}><Label text="Pronouns"/><TextInput value={pronouns} onChangeText={setPronouns} style={styles.input} placeholder="they/them" placeholderTextColor={colors.muted}/></View><View style={{width:92}}><Label text="Age"/><TextInput value={age} onChangeText={setAge} style={styles.input} keyboardType="number-pad" placeholder="31" placeholderTextColor={colors.muted}/></View></View><Label text="Occupation"/><TextInput value={occupation} onChangeText={setOccupation} style={styles.input} placeholder="Musician" placeholderTextColor={colors.muted}/><Label text="About you"/><TextInput value={about} onChangeText={setAbout} style={[styles.input,styles.multiline]} multiline placeholder="How you see yourself in Kivelle" placeholderTextColor={colors.muted}/><Label text="Interests"/><TextInput value={interests} onChangeText={setInterests} style={styles.input} placeholder="Jazz, travel, food" placeholderTextColor={colors.muted}/><Text style={styles.note}>{existing?'Updates apply to the Life or Lives already using this Persona. They never merge separate relationship histories.':'This Persona can start a separate Alternate Life. It will never replace who you are in an existing relationship.'}</Text><GradientButton label={busy?'Saving…':existing?'Save persona':'Create persona'} disabled={busy||!name.trim()||(age!==''&&Number(age)<18)} onPress={()=>void save()}/>{existing&&!existing.is_default?<Pressable onPress={remove} disabled={busy} style={styles.delete}><Trash2 size={16} color={colors.danger}/><Text style={styles.deleteText}>Delete unused persona</Text></Pressable>:null}</Screen>}
const Label=({text}:{text:string})=><Text style={styles.label}>{text}</Text>;const styles=StyleSheet.create({header:{flexDirection:'row',alignItems:'center',gap:14},subtitle:{color:colors.muted,fontSize:12,marginTop:3},label:{color:colors.muted,fontSize:11,fontWeight:'800',marginTop:3},input:{minHeight:52,paddingHorizontal:14,paddingVertical:11,color:colors.text,backgroundColor:colors.surface,borderWidth:1,borderColor:colors.border,borderRadius:radius.md},multiline:{height:100,textAlignVertical:'top'},row:{flexDirection:'row',gap:10},note:{color:colors.muted,fontSize:12,lineHeight:18,padding:12,borderRadius:radius.md,backgroundColor:colors.elevated},delete:{minHeight:48,flexDirection:'row',alignItems:'center',justifyContent:'center',gap:8},deleteText:{color:colors.danger,fontWeight:'800'}});

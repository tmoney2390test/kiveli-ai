import { Alert, Pressable, StyleSheet, Text, View } from 'react-native';
import { Image } from 'expo-image';
import { router, useLocalSearchParams } from 'expo-router';
import { ArrowLeft, RefreshCw, Trash2 } from 'lucide-react-native';
import { ErrorState, LoadingSkeleton } from '../../src/components';
import { colors, radius, spacing } from '../../src/theme';
import { useTogether } from '../../src/store/useTogether';
import { manageMedia } from '../../src/lib/api';

export default function MediaViewer(){
  const {id}=useLocalSearchParams<{id:string}>();const{snapshot,refresh}=useTogether();const media=snapshot?.generatedMedia?.find((item)=>item.id===id);const character=snapshot?.characters.find((item)=>item.id===media?.character_instance_id);const meta=media?.metadata??{};
  if(!snapshot)return <LoadingSkeleton label="Opening the photo…"/>;
  if(!media)return <ErrorState message="This photo is no longer part of your story."/>;
  const retry=async()=>{await manageMedia({action:'retry',mediaId:media.id});await refresh();};
  const remove=()=>Alert.alert('Remove this photo?','It will disappear from your Gallery and any Kivelle history that uses this image.',[{text:'Cancel',style:'cancel'},{text:'Remove photo',style:'destructive',onPress:()=>void manageMedia({action:'remove',mediaId:media.id}).then(()=>refresh()).then(()=>router.back()).catch((error)=>Alert.alert('Could not remove photo',error instanceof Error?error.message:'Please try again.'))}]);
  return <View style={styles.screen}><View style={styles.header}><Pressable onPress={()=>router.back()} style={styles.icon}><ArrowLeft color={colors.text}/></Pressable><View style={{flex:1}}><Text style={styles.name}>From {character?.together_character_templates.name??'your companion'}</Text><Text style={styles.meta}>{new Date(media.created_at).toLocaleString()}</Text></View><Pressable onPress={remove} style={styles.icon}><Trash2 color={colors.danger}/></Pressable></View><View style={styles.stage}>{media.status==='ready'&&media.signed_url?<Image source={{uri:media.signed_url}} style={StyleSheet.absoluteFill} contentFit="contain"/>:<View style={styles.state}><Text style={styles.stateTitle}>{media.status==='failed'?'That photo didn’t come through':'Photo on the way'}</Text><Text style={styles.meta}>{media.failure_reason_safe??'You can keep exploring while it finishes.'}</Text>{media.status==='failed'?<Pressable onPress={()=>void retry()} style={styles.retry}><RefreshCw size={16} color="#fff"/><Text style={styles.retryText}>Try again</Text></Pressable>:null}</View>}</View><View style={styles.details}><Text style={styles.summary}>{String(meta.sceneSummary??'A photo from your shared story.')}</Text><Text style={styles.meta}>{[String(meta.shotType??''),String(meta.timeOfDay??'')].filter(Boolean).join(' · ')}</Text></View></View>;
}
const styles=StyleSheet.create({screen:{flex:1,backgroundColor:'#05070D'},header:{paddingTop:48,paddingHorizontal:spacing.md,paddingBottom:12,flexDirection:'row',alignItems:'center',gap:12,borderBottomWidth:1,borderBottomColor:colors.border},icon:{width:42,height:42,borderRadius:21,alignItems:'center',justifyContent:'center',backgroundColor:colors.surface},name:{color:colors.text,fontWeight:'800'},meta:{color:colors.muted,fontSize:11,marginTop:3},stage:{flex:1,minHeight:360,alignItems:'center',justifyContent:'center'},state:{alignItems:'center',gap:10,padding:24},stateTitle:{color:colors.text,fontFamily:'Georgia',fontSize:24},retry:{marginTop:8,minHeight:44,paddingHorizontal:18,borderRadius:radius.pill,backgroundColor:colors.rose,flexDirection:'row',alignItems:'center',gap:8},retryText:{color:'#fff',fontWeight:'800'},details:{padding:spacing.lg,paddingBottom:34,borderTopWidth:1,borderTopColor:colors.border},summary:{color:colors.text,fontSize:14,lineHeight:21}});

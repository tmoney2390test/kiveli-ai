import{describe,expect,it}from'vitest';
import{modelFamilyFor,resolveCharacterMediaBoundaries,resolveMediaContentPolicy,routeMediaGeneration,type MediaRouteCapability}from'./media-routing.ts';

const registry:MediaRouteCapability[]=[
  {id:'wavespeed-max',provider:'wavespeed',model:'wavespeed-ai/flux-kontext-max/multi',modelFamily:'flux',mediaTypes:['image'],contentLevels:['standard','romance'],supportsCharacterReference:true,supportsLocationReference:true,maxReferenceImages:5,supportsLoRA:false,loraModelFamilies:[],supportsImageEditing:true,supportsImageToVideo:false,qualityTiers:['economy','standard','premium'],priority:97,enabled:true,asynchronous:true,preferredForQualityRetry:true,requiresReferenceImages:true},
  {id:'wavespeed-pro',provider:'wavespeed',model:'wavespeed-ai/flux-kontext-pro/multi',modelFamily:'flux',mediaTypes:['image'],contentLevels:['standard','romance'],supportsCharacterReference:true,supportsLocationReference:true,maxReferenceImages:5,supportsLoRA:false,loraModelFamilies:[],supportsImageEditing:true,supportsImageToVideo:false,qualityTiers:['economy','standard','premium'],priority:98,enabled:true,asynchronous:true,preferredForUserRequests:true,requiresReferenceImages:true},
  {id:'wavespeed-multiref',provider:'wavespeed',model:'wavespeed-ai/flux-kontext-dev/multi-ultra-fast',modelFamily:'flux',mediaTypes:['image'],contentLevels:['standard','romance'],supportsCharacterReference:true,supportsLocationReference:true,maxReferenceImages:4,supportsLoRA:false,loraModelFamilies:[],supportsImageEditing:true,supportsImageToVideo:false,qualityTiers:['economy','standard','premium'],priority:100,enabled:true,asynchronous:true},
  {id:'wavespeed-z-lora',provider:'wavespeed',model:'wavespeed-ai/z-image/turbo-lora',modelFamily:'z-image',mediaTypes:['image'],contentLevels:['standard','romance'],supportsCharacterReference:false,supportsLocationReference:false,maxReferenceImages:0,supportsLoRA:true,loraModelFamilies:['z-image'],supportsImageEditing:false,supportsImageToVideo:false,qualityTiers:['standard','premium'],priority:90,enabled:true,asynchronous:true},
  {id:'openai',provider:'openai',model:'gpt-image-2',modelFamily:'openai-image',mediaTypes:['image'],contentLevels:['standard','romance'],supportsCharacterReference:true,supportsLocationReference:true,maxReferenceImages:2,supportsLoRA:false,loraModelFamilies:[],supportsImageEditing:true,supportsImageToVideo:false,qualityTiers:['economy','standard','premium'],priority:40,enabled:true,asynchronous:false},
];

const input={mediaType:'image' as const,contentLevel:'standard' as const,qualityTier:'standard' as const,shotType:'scene' as const,characterIdentityAvailable:true,characterLoRAAvailable:false,locationReferenceAvailable:true,outfitReferenceAvailable:false,source:'user_request',userTier:'free',preferredProvider:'wavespeed'};

describe('media routing',()=>{
  it('prefers the quality route for an intentional user request',()=>{const route=routeMediaGeneration(input,registry);expect(route?.capability.id).toBe('wavespeed-pro');expect(route?.reasonCode).toBe('character_plus_location_references');});
  it('never routes suggestive, mature, or explicit media to a provider',()=>{expect(routeMediaGeneration({...input,contentLevel:'suggestive'},registry)).toBeNull();expect(routeMediaGeneration({...input,contentLevel:'mature'},registry)).toBeNull();expect(routeMediaGeneration({...input,contentLevel:'explicit'},registry)).toBeNull();});
  it('escalates a rejected candidate to the high-fidelity retry route',()=>expect(routeMediaGeneration({...input,qualityRetry:true},registry)?.capability.id).toBe('wavespeed-max'));
  it('keeps automatic media on the fast route',()=>{const route=routeMediaGeneration({...input,source:'life_event'},registry);expect(route?.capability.id).toBe('wavespeed-multiref');});
  it('does not select the multi-reference Pro endpoint without a reference',()=>{const route=routeMediaGeneration({...input,characterIdentityAvailable:false,locationReferenceAvailable:false},registry);expect(route?.capability.id).not.toBe('wavespeed-pro');});
  it('refuses every character-photo route when a usable identity reference is required but missing',()=>expect(routeMediaGeneration({...input,characterIdentityAvailable:false,locationReferenceAvailable:true,requiresCharacterReference:true},registry)).toBeNull());
  it('excludes text-only and LoRA-only routes when character photos require an identity image',()=>{const route=routeMediaGeneration({...input,requiresCharacterReference:true},registry);expect(route?.fallbacks.map((item)=>item.id)).not.toContain('wavespeed-z-lora');});
  it('treats a user edit as an intentional request and requires a true image-edit route',()=>{const route=routeMediaGeneration({...input,source:'user_edit',requiresImageEditing:true},registry);expect(route?.capability.id).toBe('wavespeed-pro');expect(route?.reasonCode).toBe('source_image_edit');expect([route?.capability,...(route?.fallbacks??[])].every((item)=>item?.supportsImageEditing)).toBe(true);});
  it('uses only a compatible LoRA family for automatic generation',()=>{const route=routeMediaGeneration({...input,source:'life_event',locationReferenceAvailable:false,characterLoRAAvailable:true,characterLoRAModelFamily:'z-image'},registry);expect(route?.capability.id).toBe('wavespeed-z-lora');const incompatible=routeMediaGeneration({...input,source:'life_event',locationReferenceAvailable:false,characterLoRAAvailable:true,characterLoRAModelFamily:'sdxl'},registry);expect(incompatible?.capability.id).not.toBe('wavespeed-z-lora');});
  it('keeps fallback ordering deterministic',()=>{expect(routeMediaGeneration(input,registry)?.fallbacks.map((item)=>item.id)).toEqual(['wavespeed-multiref','wavespeed-max','openai','wavespeed-z-lora']);});
  it('identifies model families conservatively',()=>{expect(modelFamilyFor('wavespeed-ai/flux-2-klein-4b/edit-lora')).toBe('flux');expect(modelFamilyFor('qwen-edit-uncensored')).toBe('qwen-image');expect(modelFamilyFor('grok-imagine-quality-edit')).toBe('grok-image');expect(modelFamilyFor('vendor/new-model')).toBe('unknown');});
});

describe('media policy',()=>{
  const policy={requestedLevel:'standard' as const,source:'user_request',automatic:false,ageVerified:true,characterAge:29,fictionalCharacter:true,realPersonRequest:false,nonConsensualRequest:false,minorRelatedRequest:false,characterAllowsRequestedLevel:true,romanceEnabled:true,suggestiveMediaEnabled:false,matureMediaEnabled:false,explicitMediaEnabled:false,adultVideoEnabled:false,mediaType:'image' as const,adultMediaFeatureEnabled:false};
  it('allows standard fictional-adult media',()=>expect(resolveMediaContentPolicy(policy).allowed).toBe(true));
  it('rejects unverified accounts and underage characters',()=>{expect(resolveMediaContentPolicy({...policy,ageVerified:false}).reasonCode).toBe('age_verification_required');expect(resolveMediaContentPolicy({...policy,characterAge:17}).reasonCode).toBe('adult_character_required');});
  it('rejects real-person and all higher-intensity generation',()=>{expect(resolveMediaContentPolicy({...policy,realPersonRequest:true}).reasonCode).toBe('real_person_likeness');expect(resolveMediaContentPolicy({...policy,requestedLevel:'suggestive',suggestiveMediaEnabled:true,adultMediaFeatureEnabled:true,automatic:true}).reasonCode).toBe('production_content_ceiling');});
  it('does not let preferences or feature flags reopen mature media',()=>{expect(resolveMediaContentPolicy({...policy,requestedLevel:'mature'}).reasonCode).toBe('production_content_ceiling');expect(resolveMediaContentPolicy({...policy,requestedLevel:'mature',matureMediaEnabled:true,adultMediaFeatureEnabled:true}).reasonCode).toBe('production_content_ceiling');});
  it('does not let adult-video permission reopen higher-intensity media',()=>{expect(resolveMediaContentPolicy({...policy,requestedLevel:'suggestive',suggestiveMediaEnabled:true,adultMediaFeatureEnabled:true,adultVideoEnabled:true,mediaType:'video'}).reasonCode).toBe('production_content_ceiling');});
  it('never lets policy or provider routing bypass real-person and consent boundaries',()=>{expect(resolveMediaContentPolicy({...policy,requestedLevel:'explicit',explicitMediaEnabled:true,adultMediaFeatureEnabled:true,realPersonRequest:true}).allowed).toBe(false);expect(resolveMediaContentPolicy({...policy,nonConsensualRequest:true}).reasonCode).toBe('consent_boundary');});
  it('keeps character-authored romance boundaries authoritative',()=>expect(resolveMediaContentPolicy({...policy,requestedLevel:'romance',characterAllowsRequestedLevel:false}).reasonCode).toBe('character_boundary'));
  it('uses the active CharacterVersion boundary before a stale template fallback',()=>{
    expect(resolveCharacterMediaBoundaries({allows_explicit:true},{allows_explicit:false})).toMatchObject({allows_explicit:true});
    expect(resolveCharacterMediaBoundaries(undefined,{allows_explicit:true})).toMatchObject({allows_explicit:true});
    expect(resolveCharacterMediaBoundaries({allows_explicit:false},{allows_explicit:true})).toMatchObject({allows_explicit:false});
  });
});

import{describe,expect,it}from'vitest';
import{applyPlaceOpinionEvidence,emptyCharacterPlacePerspective,mergeAuthoredPlacePerspective,placeSentimentLabel}from'./place-perspective.ts';

describe('character place perspectives',()=>{
  it('forms an opinion from explicit high-confidence evidence',()=>{const next=applyPlaceOpinionEvidence(undefined,{sentiment:.8,confidence:.9,summary:'Likes the piano-side booths when the room is quiet.',tags:['quiet','music'],favoriteDetails:['piano-side booths'],dislikedDetails:[],source:'chat'});expect(next.evidenceCount).toBe(1);expect(next.sentiment).toBeGreaterThan(.7);expect(next.opinionSummary).toContain('piano-side');});
  it('does not promote weak or empty evidence',()=>{const initial=emptyCharacterPlacePerspective();expect(applyPlaceOpinionEvidence(initial,{sentiment:1,confidence:.2,summary:'Maybe.',tags:[],favoriteDetails:[],dislikedDetails:[],source:'chat'})).toEqual(initial);});
  it('lets contradictory experience change a view gradually',()=>{const established={sentiment:.75,confidence:.82,opinionSummary:'Usually loves the live music.',opinionTags:['music'],favoriteDetails:['small stage'],dislikedDetails:[],evidenceCount:5};const next=applyPlaceOpinionEvidence(established,{sentiment:-.8,confidence:.9,summary:'The last few crowded nights have made the room less appealing.',tags:['crowded'],favoriteDetails:[],dislikedDetails:['late-night crowd'],source:'scene'});expect(next.sentiment).toBeGreaterThan(0);expect(next.sentiment).toBeLessThan(established.sentiment);expect(next.dislikedDetails).toContain('late-night crowd');});
  it('allows learned experience to override authored copy without erasing its details',()=>{const authored={sentiment:.5,confidence:.8,opinionSummary:'Likes the design.',opinionTags:['design'],favoriteDetails:['front windows'],dislikedDetails:[],evidenceCount:0};const learned={sentiment:-.4,confidence:.75,opinionSummary:'Finds it too crowded after work.',opinionTags:['crowded'],favoriteDetails:[],dislikedDetails:['evening line'],evidenceCount:4};const merged=mergeAuthoredPlacePerspective(authored,learned);expect(merged.opinionSummary).toBe(learned.opinionSummary);expect(merged.favoriteDetails).toContain('front windows');expect(placeSentimentLabel(merged.sentiment)).not.toBe('favorite');});
});
